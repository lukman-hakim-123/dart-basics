void main() {
  // operator aritmatika +-*/%
  int c = 6;
  c = c + 6;

// Operator Relasional > < >= <= != ==
  String kota = 'Jepara';
  if (kota == 'Jepara') ;

// Operator Cek Tipe Data as is is!

// Operator Logika ! || &&
  var A = true;
  var B = false;
  var expr = A && B;
  print(expr);
}
